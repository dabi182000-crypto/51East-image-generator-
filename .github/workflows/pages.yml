# Publishes the browser-only Product Image Formatter (./formatter) to GitHub Pages.
#
# Only that directory is uploaded. The Next.js generator in this repository is a
# server application — it needs an OpenAI key and a running Node process, so it
# is deployed with render.yaml / the Dockerfile instead, never to Pages.
#
# Enable once under Settings -> Pages -> Source: "GitHub Actions".
name: Deploy formatter to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "formatter/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish; queue at most one more behind it.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./formatter
      - id: deployment
        uses: actions/deploy-pages@v4
